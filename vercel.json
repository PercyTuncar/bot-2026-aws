{
  "buildCommand": "cd panel-web && npm install && npm run build",
  "outputDirectory": "panel-web/.next",
  "installCommand": "cd panel-web && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/panel-web/$1"
    }
  ]
}
